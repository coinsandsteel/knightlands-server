export class Pet {
  /*
  - .hp = ???
  - .armor = 0
  - .tier = 1-5
  - .level = 1-3
  - .penalty = false
  - .handleDamage(amount) > .modifyHP() > .modifyArmor()
  - .upgradeHP(amount) > .modifyHP(amount) > .modifyMaxHP(amount)
  - .modifyHP(amount)
  - .modifyMaxHP(amount)
  - .modifyArmor(amount)
  */
}